{
  "name": "ferie",
  "private": true,
  "scripts": {
    "dev": "cd ferie-laravel && npm run dev",
    "build": "cd ferie-laravel && npm run build",
    "serve": "cd ferie-laravel && php artisan serve",
    "start": "cd ferie-laravel && npm run start"
  }
}
